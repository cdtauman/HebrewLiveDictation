# Benchmark samples

Drop paired files here:

- `<name>.wav` — 16-bit mono PCM, 16 kHz
- `<name>.txt` — reference transcript (UTF-8)

Audio is **not** committed to the repository (privacy). This folder is kept only
for its README; add your own local samples before running the benchmark.
